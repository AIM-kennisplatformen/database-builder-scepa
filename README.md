# database-builder
Database builder, standalone app that updates the active data-sources for studio


# how to run
Make sure .env exists and is filled in.
1. ```cd src/frontend && npm run build```
2. ```pixi run database_builder```
